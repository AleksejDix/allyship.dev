import { generateMetadata } from "@/lib/metadata"

export const metadata = generateMetadata({
  title: "Products",
  description:
    "Accessibility tools and solutions to help make your website more inclusive",
  path: "/products",
})

// ... existing code ...
