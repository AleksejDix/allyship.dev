# Slack OAuth App Credentials
# Create your Slack app at: https://api.slack.com/apps
SLACK_CLIENT_ID=your_client_id_here
SLACK_CLIENT_SECRET=your_client_secret_here

# Application URL (for OAuth redirect)
APP_URL=http://localhost:3001

# Slack OAuth Scopes (what permissions the app requests)
# Recommended scopes for security scanning:
# - channels:read (read public channels)
# - groups:read (read private channels)
# - users:read (read user list)
# - users:read.email (read user emails)
# - team:read (read workspace info)
# - admin.teams:read (read workspace settings - requires admin)
