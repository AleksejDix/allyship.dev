export type Course = {
  id: string
  title: string
  videoUrl: string
  transcript: string
  // ... other course properties
}

export type Lesson = {
  id: string
  title: string
  // ... other lesson properties
}
